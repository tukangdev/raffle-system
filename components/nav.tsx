import React from "react";

const Nav = () => (
  <nav className="w-full h-16 bg-primary">
    <div className="flex flex-row justify-end items-center lg:container mx-auto h-full">
      <a className="text-white">LOG OUT</a>
    </div>
  </nav>
);

export default Nav;
